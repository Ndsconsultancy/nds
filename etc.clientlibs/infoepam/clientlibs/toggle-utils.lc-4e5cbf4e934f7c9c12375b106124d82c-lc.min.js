(function (b) {
  "object" === typeof module && module.exports
    ? (module.exports = b())
    : ((window.SlingFeature = window.SlingFeature || {}).Toggles = b());
})(function () {
  var b = null;
  return {
    isEnabled: function (c) {
      var a;
      (a = b) ||
        ((a = new XMLHttpRequest()),
        a.open(
          "GET",
          Granite.HTTP.externalize("/info/feature/toggle.json"),
          !1
        ),
        a.send(null),
        (a = 200 === a.status ? JSON.parse(a.responseText) : null));
      b = a;
      return (b || { enabled: [] }).enabled.includes(c);
    },
  };
});
